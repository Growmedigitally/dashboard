Dashboard
Website
CRM
Analytics
Reports
Ecommerce
Settings
|
|
|
Profile
Settings
    Theme
    Sidebar



    
    // fetch(Icon)
        //     .then((response) => response.text())
        //     .then((svgText) => {
        //         debugger
        //         // Parse the SVG text into a Fabric.js object
        //         const parser = new DOMParser();
        //         const svgDoc = parser.parseFromString(svgText, 'image/svg+xml');
        //         const svgElement = svgDoc.documentElement;
        //         const svgXml = new XMLSerializer().serializeToString(svgElement);
        //         const svgUrl = `data:image/svg+xml;utf8,${encodeURIComponent(svgXml)}`;

        //         fabric.loadSVGFromURL(svgUrl, (objects, options) => {

        //             const svgObject = fabric.util.groupSVGElements(objects, options);

        //             // Customize SVG object properties if needed
        //             const center = canvas.getCenter();
        //             svgObject.set({
        //                 left: center.left / 2,
        //                 top: center.top / 2,
        //                 scaleX: 1,
        //                 scaleY: 1,
        //             });
        //             if (category.id == 'gi' || category.id == 'bs') {
        //                 svgObject.scale(0.3)
        //             } else {
        //                 svgObject.scale(5)
        //             }
        //             // Add the SVG object to the canvas
        //             canvas.add(svgObject);
        //             canvas.setActiveObject(svgObject);
        //             canvas.renderAll();
        //             rerenderCanvas(canvas)
        //         });

        //         // const pathObject = fabric.util.groupSVGElements(fabric.util.parseSVGDocument(svgDoc.documentElement).childNodes, {});
        //         // canvas.add(pathObject);
        //         // canvas.setActiveObject(pathObject);
        //         canvas.renderAll();
        //     })
        //     .catch((error) => {
        //         console.error('Error loading SVG:', error);
        //     });

        // const svgElement: any = getSvgElement(Icon);
        // const svgXml = new XMLSerializer().serializeToString(svgElement);
        // const svgUrl = `data:image/svg+xml;utf8,${encodeURIComponent(svgXml)}`;
